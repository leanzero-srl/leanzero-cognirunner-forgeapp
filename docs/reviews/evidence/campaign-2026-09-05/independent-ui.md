VERDICT: PASS for the completed editor, history, Test Run and actual theme surfaces on development 22.159.0. Event-sample token redaction remains pending a separate fixed-build proof.

GATE: campaign-ui-live.mjs exited 0: 128 editor visits, 3472 assertions, zero mismatches, zero page errors. campaign-history-ui-live.mjs exited 0: eight scoped-history combinations across J02/J03/J06/J09 and both iframe style modes, plus normal pointer Delete/Cancel and Settings navigation. Scratch Test Run proof exited 0 with three exact thrown-value failures and saved-draft preservation. Actual Jira theme proof exited 0 with eight table/history cases across real product Light/Dark preferences and 1024/1440 viewports. Root owns the whole-repository syntax, offline, builds, Forge lint and deployment gates.

EVIDENCE: evidence.json contains exact live resolver configs alongside each visible field and count; history.json contains exact perIssue outcome chips, failure reasons, and 624px iframe/document width checks. test-run.json captures real testPostFunction responses for null, 42 and a string marker, visible FAIL verdicts, unchanged saved L20 functions/update timestamp after reopening, and zero saveListener requests. theme-live.json records the actual Jira preference, shell/iframe propagation and restoration to original Light after reload/reopening. Screenshots actual-theme-dark-1024-Scheduled-Jobs.png and history-J06-light.png were inspected; scoped failures remain visibly failed while successful issue chips remain green. Root's independent Jira REST receipts establish the underlying writes; UI matching alone is not treated as write proof.

LOGS: Forge logs were pulled after each live leg. full-ui-forge.log contains only the intentional L20, J06, J08 and concurrency-test failures; no UI resolver exception or unexpected recovery/throttle. theme-live-forge.log contains successful configuration reads and the idle scheduler tick. Browser HTTP404s captured with paths in theme-live.json are Jira upsell/nudge user-property endpoints. Original full-editor console entries lacked URL attribution, so those five entries are not retroactively claimed to have individually matched the later requests.

UNPROVEN: The 128-editor matrix forces the iframe's style attribute while Jira remains light; eight additional representative cases use the actual Jira preference and prove real theme propagation. The sample payloadHint is code-generator input, not an editor-rendered hint. Attachment sample UI matched stored data exactly and exposed correct fileName/issueId fields, but L13's 15-minute sampled event was earlier attachment11495 briefing.md, while L14 showed current11496. Raw contextToken visibility in the sample is a confirmed separate finding, awaiting the coordinator's fix and new deployment; evidence JSON has been sanitized. No claim covers every possible listener event or admin configuration.

HARNESS CORRECTIONS: Preserved first attachment attempt expected latest11496 despite capture throttling; second expected campaign filename despite event-type-wide sampling. Corrected shape/readback proof records the actual sample identity and timestamp. Preserved first actual-theme attempt targeted nonexistent h1 rather than the real h2.title; restoration still passed, then the corrected actual-product test passed. These setup failures are not product regressions.
