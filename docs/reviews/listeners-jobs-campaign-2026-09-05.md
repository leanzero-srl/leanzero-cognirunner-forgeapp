# Extended listener and scheduled-job campaign

Status: PASS, final development22.160.0. The runtime and UI acceptance replay passed on22.159.0 (runtime through e2a7aee; rebuilt UI3881b8a).22.160 adds only the sample-boundary fix5f377e0, verified against both legacy and freshly captured live samples. Final REST/Jira cleanup and actual UI absence checks passed. The original22-listener/10-job campaign ran on22.158.0 and exposed the defects below; its first pass alone was not treated as release acceptance. User explicitly requested at least15–20 listeners and10 jobs, actual triggers, Forge logs and UI verification, plus instance provisioning through REST. This does not claim all68 catalog events have passed.

The reproducible campaign is `test-harness/scripts/listeners-jobs-campaign.mjs`, with persisted phase state in `test-harness/results/listeners-jobs-campaign/state.json`. Commands are `provision`, `listeners`, `jobs`, `snapshot`, `cleanup`. Rules/fixtures stay present for independent live UI inspection before cleanup. Each command revokes its own temporary token; execution phases disable owned rules even after a caught transport error. Existing TPP listeners and existing scheduled jobs are preserved.

Listener coverage: issue creation/type filter, summary/priority field filters, assignment, regex comment filtering, semantic gate accept/reject, runtime AI actions, deleted issue/comment, worklog create/update/delete, attachment create/delete, link create/delete, version creation/release, component creation, deliberate step failure, simulation, disabled then enabled delivery. Every accepted event needs a fresh persisted execution plus exact Jira property/field/comment evidence. Intentional rejection/failure is a passing scenario only when its expected outcome and lack of unintended writes are proven.

Job coverage: explicit global target/context, two-issue scope, cap excluding a visible third issue, empty scope, invalid JQL, partial failure, simulated writes, missing-current-issue failure, scoped AI and global AI with explicit target. All ten first run manually while disabled, then all ten are enabled for genuine platform scheduling and disabled after their scheduled result. Negative targets and per-issue statuses are reread after scheduling; manual execution cannot substitute for the scheduled receipt.

REST coverage: single-object POST201 for all32 rules, full independent GET, collection membership, API ownership, PUT preserving nested siblings and identity, enable/disable, unauthorized creation rejection, and final DELETE followed by404 and collection absence. UI proof compares actual saved lists/editors/history against the exact rule IDs/configuration and covers both themes. Test counts and failures will be retained in the final evidence, including any changes needed to the application or harness.

Before evidence is retained in [the committed evidence directory](evidence/campaign-2026-09-05/before.json), including the original failed assertions rather than erasing them. Those two initial harness failures were corrected: Forge attachment events use `fileName` while REST uses `filename`; scripts intentionally continue after a failed step. Both corrected scenarios were retriggered and independently verified. The actual scheduled tick queued10 due jobs at17:51UTC for17:50UTC, with20 manual/scheduled terminal results and exact Jira readbacks. The initial UI inspection covered128 editor visits (all32 in two themes and two widths),3204 field assertions and256 viewport captures. It exposed hidden scoped outcomes and a624px iframe expanding to882px.

Confirmed changes and verification:

| Finding | Change and evidence |
|---|---|
| F-040 saved history hid scoped outcomes | dc6e6ad reuses the existing renderer for `perIssue` and `issues`;26 independent browser assertions. |
| F-041 failure identity and learning | 8d86527 and6f410d9 select the first actual failing index and bounded executed code, including duplicate names and offloaded bundles.53b0ae4 explains the existing continuation/no-rollback behavior. |
| F-042 arbitrary thrown values | 311ab5a safely reports strings, numbers, null and hostile objects in execution and Test Run;30 actual handler/resolver cases plus independent controls. |
| F-043 attachment context | 5048d69 derives correct Forge hints and labels from one definition; raw payload remains unchanged. |
| F-044 narrow-frame actions/navigation | 673f20d and1a3de01 keep table scrolling inside the card and wrap navigation;54 independent narrow-frame checks. b12a8ab corrects the admin subtitle. |
| F-045 concurrent counters | 52da1e7 through e2a7aee serialize accounting on the existing queue while runs stay parallel. Durable receipts, atomic count/application, recovery, generation-safe atomic deletion and batched history clearing;25 actual regression scenarios. Independent BREAK reproduced and refuted double-counting during ambiguous writes/clearing, partial deletion on receipt failure and100-push resolver timeout. |
| F-046 stale replay assertions | 0df5298 requires exact fresh manual/scheduled property values, new comment IDs and absent-label preconditions.26 independent stale/fresh controls. |
| F-047 sample token exposure | 5f377e0 removes exact `contextToken` keys recursively through the shared sample capture/read boundary, including legacy cached rows. Original runtime events remain intact. Six actual module regressions and five independent controls cover REST, resolver, capture, queue and actual sandbox. Legacy UI retains its exact capture metadata and attachment identity. Harness negative controls also corrected unsafe assertion output and vacuous attachment-identity comparisons in41833a7/86b9af7. |

The final combined code passes52/52 offline suites, backend/shared syntax and Forge lint. Both affected UIs were rebuilt with matching shared component copies. Independent BREAK is [retained here](evidence/campaign-2026-09-05/independent-break.md), with a [separate final sample-boundary pass](evidence/campaign-2026-09-05/sample-break.md).

REST provisioning is proven on all32 records: single POST201/full GET and complete collection membership; partial PUT preserves siblings/identity; enable/disable;13 additional auth/invalid-write/revocation checks. Bulk replay of22 listeners and10 jobs preserves full configuration/ownership with no duplicates; mixed valid/invalid arrays return207 with the exact rejected index. Every temporary token is revoked in its phase's finally block. Public DELETE/404 and final UI absence passed. [Auth contract](evidence/campaign-2026-09-05/rest-contract.json), [bulk replay](evidence/campaign-2026-09-05/rest-bulk.json).

Final runtime replay passed: all ten original job records were retained in the evidence and deleted with independent404 reads, then recreated with zero counters. All ten fresh manual runs and all ten genuine scheduled runs passed their expected outcomes. The actual18:43UTC Forge tick queued10 due jobs for18:40UTC; every job now has exactly two completed logs, two runs, correct errors/latest outcome and applied accounting receipts whose completion timestamp equals the log timestamp. J05/J06/J08 deliberately fail, including partial failure and missing implicit context; failure is the expected result. Twenty additional temporary listeners received two real comments on separate fixture issues, producing40 concurrent completions below the existing per-issue brake. All40 exact effects/expected errors/counters/applied receipts passed; all20 temporary records were then deleted and independently read as404. [Concurrency receipts](evidence/campaign-2026-09-05/listener-concurrency.json), [actual Forge logs](evidence/campaign-2026-09-05/forge-after-runtime.log), [multi-error live replay](evidence/campaign-2026-09-05/failure-live.json).

Final UI replay passed128 editor visits and3472 assertions across all32 saved configurations, two viewport widths and both iframe style modes. Exact settings, scripts, AI actions, schedule previews, filters, counts and error tooltips were compared against full live resolver records. Eight scoped-history combinations expose exact outcomes for J02/J03/J06/J09, including J06's successful first/third issues and failed middle issue. Normal pointer Delete/Cancel and navigation work at the624px iframe width without document overflow. Three real Test Run cases (`throw null`,42 and a string) show the exact failure while the saved L20 draft remains unchanged. Eight additional representative checks changed Jira's actual profile theme, reloaded and verified propagation to the app; original Light preference was independently restored. The128 matrix itself forces iframe styles, so it is not mislabeled as128 actual profile-theme changes. The coordinator personally inspected J06 light/dark and Test Run screenshots. [Full UI matrix](evidence/campaign-2026-09-05/ui-after.json), [independent UI verdict and limits](evidence/campaign-2026-09-05/independent-ui.md), [actual Jira themes](evidence/campaign-2026-09-05/ui-actual-theme.json).

Security checks after5f377e0: runtime and full npm audit both report0 vulnerabilities. Semgrep scanned32 backend/manifest files with0 findings/errors, validated by a separate3-finding positive canary. These pattern scans do not establish event-sample privacy; the measured legacy exposure and independent/live boundary tests do. The generated scanner profile was excluded because its generic inference/hosting prose incorrectly describes this BYOK app and its declared egress/webtriggers. [SAST](evidence/campaign-2026-09-05/security-semgrep.json), [canary](evidence/campaign-2026-09-05/security-canary.json), [runtime audit](evidence/campaign-2026-09-05/security-audit-runtime.json).

Final sample acceptance passed on22.160: both legacy attachment samples retained their original18:32/18:33 capture timestamps and exact nested attachment identity while omitting the token in public REST, the resolver and rendered UI. A new REST-created listener received real attachment11497, downloaded42 exact bytes and wrote only `tokenPresent:true`, filename and issueId to a Jira property. This proves the original runtime event was preserved without retaining the credential in evidence. Its fresh18:57:42 sample omits the token in REST and actual UI; the coordinator inspected the screenshot. The temporary listener and attachment were independently deleted/read404. [Live receipt](evidence/campaign-2026-09-05/sample-live.json), [fresh UI receipt](evidence/campaign-2026-09-05/ui-sample-fresh.json), [screenshot](evidence/campaign-2026-09-05/sample-after.png).

Cleanup passed at19:00:27UTC:22 campaign listeners,10 fresh jobs,6 fixture issues, one version and one component were deleted and independently read404. The earlier10 job records,20 concurrency listeners and one sample listener had already passed separate DELETE/404 checks. Both final collections preserve the original3 listeners and2 jobs with every non-stat metadata field unchanged. Actual UI searches show no campaign records, and clearing search restores the exact original IDs/names. Execution history is retained. [Final state with all197 checks](evidence/campaign-2026-09-05/final-state.json), [final UI cleanup](evidence/campaign-2026-09-05/ui-cleanup.json). The two original harness failures remain visible in those197 checks and are explicitly resolved by the retriggered L13/L14 and L20 cases; they were not erased or counted as passing.

No production, manifest, scope, provider or license changes were made. Historical counters are not backfilled: the statistics fix is proven on freshly created records. Existing platform limitations are retained in the earlier review; this campaign does not convert unsupported JSM request-type delivery or UI-only Assets field configuration into a passing claim. The retained development ledger now has44 fixed,3 platform-parked,0 scheduled/open findings across47 rows; this campaign closes F040–F047.
