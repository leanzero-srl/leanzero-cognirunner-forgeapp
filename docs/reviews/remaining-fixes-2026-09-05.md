# Remaining review fixes — 5 September 2026

Continuation of [the listener/job review](listeners-jobs-2026-09-05.md). The owner authorized implementing all confirmed fixes and exercising the live harness on wolfaenpak. Final application source is `d97f90b`, development deployment **22.158.0**; harness regression additions are `f60f26b`. No production deployment, manifest change, provider change or new scopes.

**Code findings: 36 fixed, 3 parked with platform evidence, 0 scheduled, 0 open across 39 retained findings. The remaining positive attachment gate now passes:** the configured external connection recovered, and a real workflow created the attachment and requested comment. Jira download matched all217 source bytes, exact document facts and a second read; temporary workflow/issue cleanup was independently verified. See the [connection recovery and final acceptance receipt](attachment-recovery-2026-09-05.md). Application deployment remains22.158.0.

## Changes and their permanent homes

| Findings | Final behavior | Commits |
|---|---|---|
| F008/F013/F014 | Workflow Test Run now uses the same production simulation as listener/job tests. Reads use Jira, writes are staged, null issue remains null, failed/empty context lookups fail visibly, empty transition IDs are rejected. Seeded docs/skills derive transition signatures from the API spec, with seed versions6/5 and disabled entries preserved. | `43c93a9`, `7d1dd83` |
| F009 | One project matcher handles slim index/full listener records and all33 non-project event types. | `6f1449f` |
| F011 | Structured `agentSummary`/`agentOutcome` survive listener, scoped and unscoped job results/logs. Existing reason prose remains compatible. | `60de7a5` |
| F018 | Shared schema/server validation rejects malformed issue/parent/other issue references before dispatch, preserving omitted bound targets and valid explicit keys/ID strings. | `9abf483` |
| F019 and lifecycle batch | Auto-minted tokens are owned, initialized once and revoked in finally; caller-supplied tokens are retained. Partial fixtures are recorded immediately, cleanup403 is a failure, successful deletes get independent404 reads. Disposable defaults use LZPT/JT and create metadata. | `20739b3`, `db1a6f6` |
| F029 | Registry orphan checks emit a bounded summary/examples, retaining records when workflow reads are uncertain. | `ea92f7b` |
| F030 | Forge API7.2.2/events2.1.7 and compatible dependency updates remove the old fetch/punycode stack; attachment multipart uses explicit binary buffer and matching boundary for the new fetch implementation. | `f303a9d` |
| F031/F032 | Simulated create/clone return distinct Jira-shaped identities owned by the simulation session. Dependent writes retain that identity; simulated issue reads never reach Jira. Denied parent/subtask/transition-name/clone reads fail instead of inventing domain results. | `2732d4f`, `c7238cf` |
| F033 | Read and upload capabilities claim atomically before consumption. Replay/concurrency cannot duplicate access; claim/delete faults fail closed without Jira dispatch. Other rule claims retain their existing availability policy. | `1b49c81` |
| F034/F035 | Partial listener creation is cleaned. Object-key proof reads real structured tool traces and current guard wording; pass/fail/inconclusive exits are honest. The attachment rejection harness also exits nonzero on a failed verdict. | `db1a6f6`, `f60f26b` |
| F036 | The complete serialized scoped log/task is bounded after construction. Every issue key/outcome remains, truncated display text is marked, oversized change-display records are counted as omitted. | `58ec8f2`, final `892ef33` |
| F037/F038 | Document creation requires explicit success and, when uploading, explicit upload success,2xx receiver status and concrete attachment ID. Local creation/ambiguous/error envelopes cannot produce “Attached”, a linking comment or downstream library copy. Shared MCP bridge preserves HTTP and tool-level `isError` failures. | `8f029ab`, `42495b6` |
| F039 | IssuePicker dismisses on bubbling click. Closing its dropdown no longer shrinks the scrolled test panel and moves Run Test between mouse-down/up. Both current copies and both rebuilt bundles match. | `d97f90b`, pointer regression `f60f26b` |

## Test evidence

Selected sanitized receipts are committed in [the evidence directory](evidence/2026-09-05-remaining/). Full independent result details are in [the tester's report](evidence/2026-09-05-remaining/independent-prove.md); that report records its original per-version scope, and this continuation supplies the final UI/attachment disposition.

- **49/49 offline suites** after F037/F038, including actual handler/resolver tests, dynamically importing shared modules, rules115/115, agent references155/155 and aggregate persistence stress cases. Syntax, Forge lint, shared component parity and both final UI builds pass. [Offline receipt](evidence/2026-09-05-remaining/offline-final.txt), [final dev deployment](evidence/2026-09-05-remaining/deploy-final.txt).
- **19/19 scheduled-job live checks** on22.154, including a real scheduled tick with independent Jira comment readback, manual/scoped runs, stats and cleanup. **23/23 resolver checks** on22.154. Rules were reread absent; temporary issues were independently404 after deletion. [Jobs](evidence/2026-09-05-remaining/jobs-e2e.json), [resolvers](evidence/2026-09-05-remaining/resolvers-live.json).
- **20/20 exact structured agent checks** on22.155/892ef33: listener, scoped and unscoped summaries/outcomes plus independently read Jira labels. Six owned tokens were independently confirmed revoked. Initial overlapping AI runs hit the model's per-minute429 limit; sequential retries passed, and initial failures remain recorded. Revoked token tombstones caused an early scratch audit false alarm, corrected using exact ownership and revocation metadata. [Final structured proof](evidence/2026-09-05-remaining/structured-22-155.json), [initial failure](evidence/2026-09-05-remaining/structured-initial-failure.json), [revocations](evidence/2026-09-05-remaining/owned-token-revocations.json).
- **JSM/Assets27 passed,0 failed,2 explicit platform skips** on22.154. A quota failure happened after one internal-note write; the retry explicitly verified a newly created second note with `public:false`, rather than counting the first partial result again. [Original receipt](evidence/2026-09-05-remaining/jsm-assets.json), [exact retry](evidence/2026-09-05-remaining/jsm-retry.json).
- **10/10 actual workflow UI checks** on22.158, independently repeated by coordinator and tester. Final ordinary mouse click starts with dropdown open and inner panel scrollTop36; down/up/click hit the same button at unchanged coordinates. Summary, labels, assignee, comments, status and issue property remained exactly unchanged in a second Jira read; JT-68/JT-69 fixtures were deleted and independently404. [Before defect](evidence/2026-09-05-remaining/pointer-before.json), [independent fix proof](evidence/2026-09-05-remaining/pointer-independent-after.json), [coordinator proof](evidence/2026-09-05-remaining/workflow-simulation-final.json), [light](evidence/2026-09-05-remaining/workflow-light.png), [dark](evidence/2026-09-05-remaining/workflow-dark.png). Final screenshots disable animation and were visually inspected.
- **9/9 live attachment capability rejection checks** on22.158. These verify rejection/no leakage, not positive upload/replay. Atomic same-capability concurrency/replay/storage-fault cases are proven offline through the actual handlers. [Live rejection receipt](evidence/2026-09-05-remaining/attachment-rejections.txt).
- Runtime and full **npm audits0**, **SAST0 findings/errors** with30 scanned files and a3-hit positive canary. Scanner outputs are committed; the generic generated technical-profile boilerplate was deliberately excluded because it incorrectly denied this app's third-party AI/egress surface. Exact binary multipart regression checks all bytes, MIME, filename, boundary, bound issue targeting and capability consumption. [Runtime audit](evidence/2026-09-05-remaining/npm-runtime-audit.json), [full audit](evidence/2026-09-05-remaining/npm-full-audit.json), [SAST](evidence/2026-09-05-remaining/semgrep.json), [canary](evidence/2026-09-05-remaining/semgrep-canary.json).
- Independent six-lens BREAK clean through892ef33 and again for F037/F038/F039 atd97f90b. Harness-only final additions were independently reviewed with a positive failure control: all HTTP200 security responses produce0/9 and exit1; expected401/404 responses produce9/9 and exit0. [Earlier final BREAK](evidence/2026-09-05-remaining/break-through-892ef33.md), [supplemental final BREAK](evidence/2026-09-05-remaining/break-final.md).

The final log window contains no new simulation errors or DEP0040 on the tested invocations. It does contain the separately documented MCP502/egress403 probes; the whole environment is not described as error-free.

The existing Forge live harness also passed its genuine admin deep walkthrough and Explain-this-rule journey on22.158 (`HEADLESS=1 npx playwright test scenarios/cognirunner/admin-ui-deep.spec.ts --project=chromium`,1/1 journey). [Receipt](evidence/2026-09-05-remaining/admin-deep.txt).

## Earlier attachment connection failure (subsequently recovered)

The actual workflow attachment harness first observed zero Jira attachments despite a success-shaped “Generated and attached” result. The shared helper accepted ambiguous `{ok:false}`/missing-success envelopes as well as the MCP's local-success/upload-failure shape. Both are now rejected. On22.156 the real workflow correctly reported failure after the document endpoint returned HTTP502. Final22.158 proof on JT-70 explicitly read zero attachments and zero comments before and after the failed workflow; the result reported `Document generation/attachment failed: MCP HTTP 502`. Temporary transition and issue were removed and reread absent. This is a passing failure-path assertion and a failed positive attachment acceptance test. [Failure receipt](evidence/2026-09-05-remaining/attachment-positive-failure.json), [initial fixed runtime](evidence/2026-09-05-remaining/attachment-failure-runtime.txt), [final runtime](evidence/2026-09-05-remaining/attachment-failure-final.txt).

The configured `https://worksmacstudio.tailfc4700.ts.net/docproc/mcp` endpoint repeatedly returned502 through the genuine authenticated admin MCP Test action. On the workhorse, the document process was healthy at loopback10000, with17 tools, and unauthenticated MCP POST returned401. Its public alternate port10000 was explicitly blocked by Forge's existing egress permissions. No permission was added. A temporary443 path to that same already-published authenticated service also returned502. The temporary path was removed; the complete Funnel configuration was compared equal to its captured baseline. The test-site MCP URL and existing bearer presence were restored and reread exactly. No server source, service restart, auth change or other route change was retained. [Port probe](evidence/2026-09-05-remaining/mcp-port-probe.json), [temporary route probe](evidence/2026-09-05-remaining/mcp-route-probe.json), [before](evidence/2026-09-05-remaining/funnel-before.json), [restored](evidence/2026-09-05-remaining/funnel-restored.json).

**Acceptance gate subsequently closed:** `node test-harness/scripts/attachment-positive-live.mjs` passed on JT-73 after public443 connectivity recovered. It created one actual attachment and exactly one requested comment, downloaded and verified the document, reread both IDs/content, then verified workflow/issue cleanup before declaring PASS. The [follow-up](attachment-recovery-2026-09-05.md) retains the intervening failures and states the exact tested scope.

## Parked platform findings and fixture restoration

F002/F006: real JT UI created, updated and deleted request type341. REST independently verified exact ID/name/description and final404. The same listener on the same project received JT-51's issue-created event and ran successfully; request-type samples stayed absent over the observation window ending15:01:54Z. This closes the prior UI coverage gap and supports nondelivery in this installed environment; it does not claim all tenants or all68 events passed. REST request-type update remains unsupported. [UI and event evidence](evidence/2026-09-05-remaining/request-type-ui.json).

F003: Assets field configuration still requires Jira UI rather than a public API; that UI configuration and subsequent canonical-identifier persistence were already exercised. This is a platform limitation, not unfinished code work.

The two continuation-owned COGTEST issues2696/2697 were positively identified and deleted after proving Delete Issues on each exact issue. The temporary project-role grant was removed and the complete prior actor list restored. Historical workflow barrage fixtures were retained. [Cleanup/restoration receipt](evidence/2026-09-05-remaining/old-fixture-cleanup.json).

Plans Start date write/read/restore was already proven on22.153 for WFH-36 in both Jira and the actual Plans row; this continuation did not rerun or change Plans behavior. That evidence remains linked from the preceding review.
